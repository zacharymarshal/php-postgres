---
engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
  fixme:
    enabled: true
  phpmd:
    enabled: true
    config:
      file_extensions: "php"
      rulesets: "phpmd-ruleset.xml"
ratings:
  paths:
  - "**.php"
exclude_paths:
- tests/
